open source agent harness

## Commands to use

- `Ctrl-O` view full outputs for reasoning and tool outputs
- `/export` export sessions into a md file
- `/sessions` view past sessions, which are stored in `~/.kimi`
- `/new` start a new session