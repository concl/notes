HTTP is a protocol that defines an interface (a set of rules for transferring information) between 2 parties: a **client** and a **server**.

Information transfer starts with an HTTP **request**, which is a message sent over TCP from the client to the server. An HTTP request generally consists of a **header** and a **payload**. 