Node.js is a javascript runtime environment, used for building apps in javascript. It consists of the V8 engine (an interpreter/JIT compiler) alongside APIs including `fs` (file system stuff), networking, modules, and the event loop.

